# This workflow adds the 'Annual check-in due' label to issues created 1, 2, and 3 years ago (365, 730, and 1095 days), then comments on the issue to notify assignees that the check-in is due

name: annual check-in label

on:
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:

jobs:
  label-issues:
    runs-on: ubuntu-latest

    permissions:
      issues: write

    steps:
      - name: add annual check-in labels and notify assignees
        uses: actions/github-script@v7
        with:
          script: |
            const checkInDays = [365, 730, 1095];

            const { data: issues } = await github.rest.issues.listForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: "open",
              per_page: 100
            });

            for (const issue of issues) {
              const created = new Date(issue.created_at);
              const now = new Date();

              const ageDays = Math.floor(
                (now.getTime() - created.getTime()) /
                (1000 * 60 * 60 * 24)
              );

              const hasCheckInLabel = issue.labels.some(
                label => label.name === "Annual check-in due"
              );

              const isCheckInDue = checkInDays.some(
                d => ageDays >= d && ageDays < d + 1
              );

              if (isCheckInDue && !hasCheckInLabel) {
                await github.rest.issues.addLabels({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: issue.number,
                  labels: ["Annual check-in due"]
                });

                const mentions = issue.assignees
                  .map(a => `@${a.login}`)
                  .join(" ");

                if (mentions) {
                  await github.rest.issues.createComment({
                    owner: context.repo.owner,
                    repo: context.repo.repo,
                    issue_number: issue.number,
                    body: `👋 ${mentions}, this grant is due an annual check-in. Email the PI (there's a saved reply!) then remove the check-in label.`
                  });
                }

                console.log(
                  `Added label and notified assignees for issue #${issue.number}`
                );
              }
            }
